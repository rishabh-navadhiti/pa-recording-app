# In-flight feature plans

One line per plan currently being worked on. When a feature ships, `git mv` its plan file into [`../archive/plans/`](../archive/plans/) and remove the line below.

Plan files use the convention `YYYY-MM-DD-<initials>-<slug>.md`.

> **Big restructure:** the codebase refactor (modularizing `main.js`/`renderer.js`, the engine framework, distribution/packaging) is a multi-PR program with its own folder — see [`../refactor/`](../refactor/) (start at its [README](../refactor/README.md)). It's not a single-feature plan, so it lives outside this table.

| Plan | Owner | Status |
|---|---|---|
| [2026-05-19-rs-cdi-v1-skill.md](2026-05-19-rs-cdi-v1-skill.md) | rs | Planned (Plan 1 of 2 — CDI skill + standards files; app integration is a separate Plan 2 after this lands) |
| [2026-05-22-rs-unify-docx-generation.md](2026-05-22-rs-unify-docx-generation.md) | rs | Planned (generate-note emits JSON manifest as final line; app owns multi-patient folder splits + all docx; no DB schema changes) |
| [2026-05-22-rs-cdi-v1-app-integration.md](2026-05-22-rs-cdi-v1-app-integration.md) | rs | Planned (Plan 2 of 2 — Phase 1 re-implements the ICD step natively on develop; Phase 2 wires CDI skill into pipeline, UI, DB. **Implementation on a new `cdi-v1` branch off latest develop; `icd10-coding` kept as read-only reference, NOT merged.**) |
| [2026-06-05-rs-costigan-procedure-checklist.md](2026-06-05-rs-costigan-procedure-checklist.md) | rs | Implemented (skill + standards only — `cdi-costigan`, a CDI variant doing procedure-specific medical-necessity validation against Cedars CRI / Medicare LCD checklists for 5 interventional-pain procedures; all ICD codes connector-validated. UI/pipeline wiring deferred to the refactor.) |
| [2026-06-11-rs-pa-engines-v0.2.md](2026-06-11-rs-pa-engines-v0.2.md) | rs | Implementing on `feature/pa-engines-v0.2` (4 deliverables, **pipeline-integrated + toggle-gated**: NEW engines+skills `em-score` (E/M MDM scorer) + `patient-summary`, registered in the chain after CDI, JSON-only, persisting to a new generic `engine_outputs` table — no `cases` column-splatter; EXTEND `cdi-review` with provider-query generation + per-flag E/M reimbursement signal (ride with `enableCdi`, zero DB work — column already wired). UI toggles added. Presentation (JSON→HTML→PDF) + clinical-orders engine deferred to next step.) |
| [2026-06-22-sr-global-model-registry.md](2026-06-22-sr-global-model-registry.md) | sr | **Design only — not yet implemented** (6 open questions). Make one provider-aware registry the single source of truth for SOAP / template / engine model selection: role-tagged options `{id,label,provider,model,roles}`, per-role defaults, renderer builds both dropdowns from it (via a `getModelOptions()` IPC), settings validates against it. Kills the 3-copy SOAP list, the hardcoded template `<option>`s, and the engine↔`soapModel` coupling (404 bug). |
| [2026-06-12-rs-engine-output-html-pdf.md](2026-06-12-rs-engine-output-html-pdf.md) | rs | **Design only — not yet implemented** (10 open questions). Move engine *review* outputs (CDI, E/M, patient-summary) off Markdown/docx onto an **HTML→PDF** path: engine JSON → reusable self-contained cockpit template (from `docs/notes/cdi-ui-reference/presentation_cockpit_scroller.html`) → Electron `printToPDF` → `<case>_<engine>.pdf` (+ hidden JSON), zero new deps. Engine-agnostic `toDocument()` descriptor hook + a fixed PDF post-step (mirrors how docx is treated). Drops `_cdi.md`/`_cdi.docx`; **keeps** the SOAP note's md→docx. Includes an engine fix: `em-score` emits structured `billed_em_code` (parsed from the note's `.KS15` placeholder) so the downcode story is data-driven, not hardcoded. PHI de-identification flagged for later.) |
| [2026-06-23-sr-edit-note-api.md](2026-06-23-sr-edit-note-api.md) | sr | **Implemented** on `feature/edit-note-api`. Convert pre-chart (edit-note skill) from `claude -p` CLI to single-call Anthropic API, mirroring the generate-note-api pattern. New `prechartApi.js` descriptor + `edit-note-api/SKILL.md` + `buildSingleCallNoteEdit()`. Includes Windows EPERM fix (strip `attrib +h` before writeFileSync on hidden .md files). |
| [2026-06-24-sr-enable-mic-capture.md](2026-06-24-sr-enable-mic-capture.md) | sr | **Planned.** Settings **"Enable microphone"** toggle (`enableMic`): record the local default mic alongside loopback and mix both (two WAVs → `pydub.overlay()` → 16k mono MP3) so anyone speaking in the room is transcribed. Mutually exclusive with realtime; mic-open failure is non-fatal (loopback-only fallback). v1 = default mic, no device picker. |
| [2026-06-24-sr-recording-prechart.md](2026-06-24-sr-recording-prechart.md) | sr | **Planned.** In-recording **Pre-chart** capture screen (sub-view of the Record tab, shown in RECORDING/PAUSED with the timer still visible): scribe types context + attaches `.md/.txt/.docx/.pdf`. Context held in `recorderController`, written to `<caseDir>/prechart.md` at stop, then fed into **initial** note generation via a NEW `generate-note-prechart-api` skill (API path only; separate from edit-note). |
| [2026-06-26-rs-costigan-cdi-api.md](2026-06-26-rs-costigan-cdi-api.md) | rs | **Implemented on `feature/costigan-cdi-api`.** Opt-in, Costigan-only **procedure checklist** as a single Anthropic-API call (port of agentic `cdi-costigan`, NO connector — packs carry validated codes). New `cdi-costigan-api/SKILL.md` (system prompt) + the 5 packs concatenated onto the system; per-case note+chart in the user message. Wired into the pre-chart `onSuccess` (gated by `enableCostiganCdi` + a Costigan doctor check); a 2nd "chart" input box appears in Pre-chart only when enabled — Box A→edit-note (unchanged), Box B chart→checklist only. Writes `_costigan.{json,md}` + `_chart_input.md`; **no DB schema change** (one `processing_events` row). HTML→PDF render deferred to the `engine-output-html-pdf` work. |

---

**To start a plan:**
1. Copy the convention above. Drop a markdown file into this folder.
2. Add a row to the table.
3. Implement against it. Update [`../../CLAUDE.md`](../../CLAUDE.md), [`../ARCHITECTURE.md`](../ARCHITECTURE.md), and [`../DECISIONS.md`](../DECISIONS.md) as part of the work — not afterwards.
4. After merge: `git mv` the plan file into `../archive/plans/`, delete its row from the table.
